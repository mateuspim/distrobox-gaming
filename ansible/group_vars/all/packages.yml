# Package lists for the gaming distrobox.

dg_pacman_packages_base:
  - base-devel
  - git
  - curl
  - wget
  - unzip
  - p7zip
  - rsync
  - which
  - sed
  - grep
  - ripgrep
  - awk
  - mesa
  - vulkan-icd-loader
  - vulkan-tools
  - libva
  - libva-utils
  - pipewire
  - pipewire-pulse
  - alsa-utils
  - sdl2
  - sdl3
  - qt5-base
  - qt5-wayland
  - qt6-base
  - qt6-wayland
  - qt6-svg
  - qt6-multimedia
  - qt6-shadertools
  - gtk3
  - gtk4
  - xdg-utils
  - desktop-file-utils
  - gamescope
  - libarchive
  - fuse2
  - fuse3
  - zlib-ng-compat
  - dolphin-emu
  - ppsspp
  - zsh
  - starship
  - retroarch
  - python-cryptography

dg_pacman_packages: >-
  {{ dg_pacman_packages_base + (['steam'] if dg_install_steam | bool else []) }}

dg_core_aur_packages:
  - duckstation-qt-bin
  # pcsx2-latest-bin tracks the rolling 2.7.x AppImage (currently 2.7.274).
  # The stable 2.6.3 (pure `pcsx2`) hard-crashes on GT3 at the car-showcase
  # screen due to a GS renderer regression that was fixed sometime in
  # 2.7.x. Previously pinned to 2.6.3 because the `pcsx2-latest-bin`
  # PKGBUILD had a URL mismatch; upstream has since corrected the build
  # (verified installing 2.7.274 successfully April 2026).
  - pcsx2-latest-bin
  - melonds-bin
  - evsieve
  - flycast-git
  - emulationstation-de

dg_advanced_aur_packages:
  - rpcs3-bin
  - eden-bin
  - cemu-bin
  - xemu-bin
  # vita3k-bin: SKIPPED. Its AUR PKGBUILD fails to build (the nightly zip
  # dropped org.vita3k.vita3k.metainfo.xml, so package() errors out) —
  # verified failing on a from-scratch rebuild 2026-08-01. Not our bug.
  # Re-enable by uncommenting once upstream restores the metainfo file; the
  # bootstrap AUR install already tolerates it failing, this just avoids
  # the wasted build attempt every run.
  # - vita3k-bin
  - shadps4-bin
  # Sega Model 3 arcade (roms_rare/model3). Plain `supermodel` is the
  # maintained snapshot package; supermodel-git is orphaned, and
  # supermodel-bin is an unrelated AI tool — do not "upgrade" to it.
  - supermodel
  # ROM / patch tools (moved off the host — no desktop use case there)
  - 3dsconv
  - extract-xiso-bin   # source -bin: the vanilla -source pkg fails to compile on modern gcc
  - flips

dg_aur_packages: >-
  {{ dg_core_aur_packages +
     (dg_advanced_aur_packages if dg_advanced_emulators_enabled | bool else []) }}

dg_xenia_pacman_packages:
  - wine
  - winetricks
