<?xml version="1.0"?>
<!-- Managed by Ansible - run reset-configs.yml to restore defaults. -->
<systemList>
{% for sys in dg_esde_systems if sys.enabled | default(true) | bool %}
    <system>
        <name>{{ sys.name }}</name>
        <fullname>{{ sys.fullname }}</fullname>
        <path>{{ sys.path }}</path>
        <extension>{{ sys.extension }}</extension>
{% for cmd in sys.commands %}
        <command label="{{ cmd.label }}">{{ dg_gpu_env + ' ' if dg_gpu_env }}{{ cmd.cmd }}</command>
{% endfor %}
        <platform>{{ sys.platform }}</platform>
        <theme>{{ sys.theme }}</theme>
    </system>
{% if not loop.last %}

{% endif %}
{% endfor %}
</systemList>
